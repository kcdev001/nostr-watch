ORM_HOST=127.0.0.1
ORM_PORT=3306
ORM_USERNAME=root
ORM_PASSWORD=your_password
ORM_DATABASE=nostr_watch

PORT=3100

# Comma-separated relay URLs
NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol,wss://relay.nostr.band,wss://relay.snort.social,wss://offchain.pub

# Relay query timeout in ms
RELAY_QUERY_TIMEOUT=8000
